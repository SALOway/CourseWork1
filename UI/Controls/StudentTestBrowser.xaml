<UserControl x:Class="UI.Controls.TestBrowser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Content="Log Out" Width="70" Height="20" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0"/>
        
        <StackPanel HorizontalAlignment="Center" Margin="0,30,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="Search..." MinWidth="250" Margin="0,0,5,0"/>
                <Button Content="Search" Width="75"/>
            </StackPanel>
            <ListView Margin="0,5,0,0">
                <ListViewItem>
                    Dick
                </ListViewItem>
                <ListViewItem>
                    Dick
                </ListViewItem>
                <ListViewItem>
                    Dick
                </ListViewItem>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
