<UserControl x:Class="UI.Views.TeacherMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Views" 
             xmlns:viewmodels="clr-namespace:UI.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:TeacherMenuViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <!--Кнопка виходу з аккаунту-->
        <Button Content="Вйти з аккаунту" Command="{Binding LogOutCommand}" FontSize="24"
                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Padding="25,5,25,5"/>

        <!--Кнопка браузеру тестів-->
        <Button Content="Браузер тестів" FontSize="24" Command="{Binding OpenTestBrowserCommand}"
                HorizontalAlignment="Center" VerticalAlignment="Center" Padding="25,5,25,5"/>
    </Grid>
</UserControl>
